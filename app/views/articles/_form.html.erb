<%= form_for @article, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :pub_med_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :pub_med_id, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :citations_page_rank, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :citations_page_rank, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                articles_path, :class => 'btn' %>
  </div>
<% end %>
