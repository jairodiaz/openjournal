<%= form_for(@article) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pub_med_id %><br />
    <%= f.text_field :pub_med_id %>
  </div>
  <div class="field">
    <%= f.label :citations_page_rank %><br />
    <%= f.text_field :citations_page_rank %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
